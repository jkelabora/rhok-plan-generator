# Run rake tasks before an application deployment
container_commands:
  01seed:
    command: rake db:seed
  02test:
    command: assets:precompile:all RAILS_ENV=production RAILS_GROUPS=assets  --trace
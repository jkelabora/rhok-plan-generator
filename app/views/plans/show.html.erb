<section class='headline plan-details'>
  <div class='row'>
    <div class='small-8 columns'>
      <div class="plan-name">
        <h4>PLAN NAME</h4>
        <h1><%= @plan.name %></h1>
      </div>

      <div class="postcode">
        <h3><i class="fi-marker"></i> Postcode <%= @plan.postcode %></h3>
      </div>
    </div>
  </div>
  <div class='row'>
    <div class='large-8 columns description'>
      <p>This is an example fire survival plan that has been developed specifically for Warrandyte.</p>
      <p>Yes, there's a lot of text here, but have a glance and if it seems close to what you'd like, click the button to make a copy which you can customise to suit your situation.</p>
    </div>
    <div class='large-4 columns use-plan'/>
      <%= link_to plan_duplicate_url(:public_guid => @plan.public_guid), method: :post, class: 'button' do %>
        <i class="fi-page-edit"></i>Edit a copy of this plan
      <% end %>
    </div>
  </div>
</section>

<section class='main-page'>
  <div class='row'>
    <div class='small-12 columns'>
      <% @plan.events.each do |event| %>
        <div class="event-name">
          <h2><%= event.name %></h2>
        </div>

        <table class="tasks">
          <tbody>
            <% @plan.tasks_for(event).each do |task| %>
              <tr>
                <td><%= task.name %></td>
              </tr>
            <% end %>
          </tbody>
        </table>

      <% end %>
    </div>
  </div>

</section>

<section class='headline'>
  <div class='row'>
    <div class='small-12 large-12 small-centered large-centered columns submit'>
      <%= link_to plan_duplicate_url(:public_guid => @plan.public_guid), method: :post, class: 'button' do %>
        <i class="fi-page-edit"></i>Edit a copy of this plan
      <% end %>
    </div>
  </div>
</section>

